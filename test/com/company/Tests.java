package com.company;

import org.junit.Assert;
import org.junit.Test;

public class Tests {

    @Test
    public void skiJumpingStarsTest() {
        int stars = 24;
        int maxStars = 24;
        int starsFactor = stars/maxStars;
        Assert.assertEquals(starsFactor,1);
    }
    @Test
    public void footballStarsTest() {
        int stars = 24;
        int maxStars = 24;
        int starsFactor = stars/maxStars;
        Assert.assertEquals(starsFactor,1);
    }
    @Test
    public void basketballStarsTest() {
        int stars = 24;
        int maxStars = 24;
        int starsFactor = stars/maxStars;
        Assert.assertEquals(starsFactor,1);
    }
    @Test
    public void volleyballStarsTest() {
        int stars = 24;
        int maxStars = 24;
        int starsFactor = stars/maxStars;
        Assert.assertEquals(starsFactor,1);
    }
    @Test
    public void tennisStarsTest() {
        int stars = 24;
        int maxStars = 24;
        int starsFactor = stars/maxStars;
        Assert.assertEquals(starsFactor,1);
    }

}